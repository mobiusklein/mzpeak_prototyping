[project]

name = "mzpeak"
version = "0.2.0"

dependencies = [
    "pyarrow",
    "pandas",
    "numpy",
    "psims",
    "pynumpress",
]

license = "Apache-2.0"

[project.optional-dependencies]
sql = ["datafusion"]